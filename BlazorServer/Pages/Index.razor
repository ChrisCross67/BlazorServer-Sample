@page "/"
@using Microsoft.Extensions.Localization


<h1>@(Localizer["Hello"])</h1>

@(Localizer["Welcome"])

<SurveyPrompt Title="@(Localizer["HowIsBlazorWorkingForYou"])" />

@code
{
    [Inject]
    public IStringLocalizer<Index> Localizer { get; set; }
}